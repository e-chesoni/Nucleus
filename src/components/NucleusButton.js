import Button from 'react-bootstrap/Button';

export const NucleusButton = ({}) => {
    return (
        <div className="project-button-container">
            <Button className="project-button">Show Me More</Button>
        </div>
    )
}